import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monitoreodeportivo.app',
  appName: 'sistema-monitoreo-deportivo',
  webDir: 'www'
};

export default config;
